---
title: "Requirements knowledge"
type: knowledge
detail: "Generated from task/archive — do not edit by hand without refresh"
last_refresh: 2026-07-13
last_refresh_mode: full
source_task_count: 11
manifest_tasks: 11
---

# Requirements knowledge

> Generated by **iom-todo-task-knowledge**. Refresh: `refresh knowledge` (incremental) · `refresh knowledge full`

## Should do

### All tasks

- State one clear **Goal** and explicit **In scope** / **Out of scope** bullets (≥2 out-of-scope items).
- Link **Related** files — prior tasks, outputs, `AGENTS.md`, `design/WORKFLOW.md`.
- Record **Assumptions**, **Constraints**, and open questions in Requirement (not only in checklist).
- Update `task/log.md` and `task/index.md` via iom-todo-task workflow on create/execute/archive.

### Design & image pipeline

- Define plants once in `design/placements.json`; project per view via `design/view-anchors.json`.
- Build **visibility matrix** before multi-view edits — enumerate which `resources/*.png` and orbit IDs are affected.
- Run mandatory hybrid pipeline: `place --guide-dir` (position guide) → `generate_image` (CGI blend).
- For `partial` views with roof/structure occlusion: `composite-foreground --view <viewId>` after blend.
- For `hidden` views: `reset-hidden` — copy raw to `*_designed.png` (no tree).
- Back up original `resources/*.png` to `resources/backup/` before overwriting designed outputs.
- Wire `designedSrc` in `outline/home.html` for every orbit viewpoint in the visibility matrix.
- Update `design/WORKFLOW.md`, image-processor SKILL, and `AGENTS.md` when pipeline rules change.

### Quotation & pricing

- Extract and **validate** PDF line items (subtotals, suspicious quantities — especially grass area/units).
- Cross-check PDF quantities against plan-derived areas; flag mismatches (e.g. grass 10 sqm vs plan 177+ sqm).
- Perform web research for market benchmark ranges before setting mid-point unit prices.
- Document benchmark prices as **labor + materials** (not government rates); note spec assumptions.
- Separate **material-only** vs **labor+install lump sum** — never mix without explicit labeling.
- Survey government sources (กรมบัญชีกลาง, TPSO, data.moc.go.th) — document what exists vs gaps; do not invent official landscaping tables.
- Add **price source citations** (URLs) in deliverable HTML when presenting benchmarks to users.
- Show comparison tables: unit price, line total, difference %, and price per sqm where relevant.
- Document area-calculation **formulas**, grid dimensions, and ±5–10% measurement uncertainty.
- When prior deliverables must stay frozen: create **new versioned files** (`v2`, `qt-v1/`) — do not edit v1.

### Layout & UI

- Group all 13 house images in `outline/home.html` with section filters and Design/Raw toggle.
- Placement Picker in **header toolbar** (not beside search); support all 13 views with per-view JSON export.

## Should not do

- **Text-only `generate_image`** for plant placement without placement guide — imprecise and non-repeatable.
- Use `place` **without `--guide-dir`** as final output on 3D renders (pasted-photo look).
- Edit a single viewpoint when a yard element is visible from multiple angles (no cross-view propagation).
- Skip occlusion logic — tree must not appear when house wall, gate, or carport blocks the camera.
- Modify frozen quotation files when Requirement says "ห้ามแก้ของเดิม" / new file only.
- Negotiate prices, hire contractors, or buy materials on behalf of the client.
- Train custom diffusion models, NeRF, or Gaussian Splatting for this repo's scope.
- Treat benchmark totals as binding quotes — always flag items needing vendor spec confirmation.
- Claim regional government landscaping price tables exist when survey shows they do not.

## Patterns by tag

| Tag | Guidance |
|-----|----------|
| `placement`, `multi-view` | Canonical site coords → per-view anchors → visibility matrix → hybrid blend |
| `occlusion`, `3d` | `design/occluders.json` + `full` / `partial` / `hidden`; composite-foreground for partial |
| `quotation`, `benchmark` | PDF extract → web research ranges → mid-point unit price → HTML comparison |
| `plan`, `area-calculation` | Grid dims from plan image → formula per color zone → document assumptions |
| `html` | Print-friendly CSS; self-contained portable summary when sharing (inline/base64 assets) |
| `workflow`, `research` | Synthesize findings in `design/WORKFLOW.md` or task body before execution sub-task |
| `southern-thailand`, `gap-analysis` | Regional web research → completeness table (ครบ/บางส่วน/ไม่พบ) → update `summary.html` columns |

## Open follow-ups

- Sync grass fix **100 sqm @ 157.5 = 15,750** from `summary.html` → `benchmark-quotation.html`, `qt-v1/*`.
- Archive task `011-southern-benchmark-pricing` then run incremental `refresh knowledge` to ingest regional patterns.
- Sync price-source citations from `summary.html` to `benchmark-quotation.html` (and optionally v2 / qt-v1).
- Confirm vendor specs on suspicious PDF lines (grass 10 sqm @ 1,575 THB/sqm; exterior paving spec).
- Tune `garage-right` occluder masks; extend foreground occlusion to `garage-front` if needed.

## Source tasks

| Slug | Type | Key lesson |
|------|------|------------|
| 000-project-bootstrap | planning | AGENTS + task index/log + Pillow verify before design work |
| 001-create-home-outline | execution | 13-view gallery template with section nav |
| 002-implement-3d-360-view | planning | Orbit dial + research before UI implementation |
| 003-add-front-right-lamsam-tree | execution | Backup originals; exposed single-view / text-only gaps |
| 004-improve-multi-view-plant-placement | planning | Placement registry + visibility matrix + `place` command |
| 005-hybrid-generative-blend | execution | 2-phase place guide → generate_image mandatory |
| 006-placement-picker-occlusion-3d | planning | All-view picker + occluders.json + composite-foreground |
| 007-analyze-landscaping-quotation | planning | PDF validation + benchmark ranges + contractor Q&A |
| 008-create-benchmark-landscaping-quotation | execution | Benchmark HTML with same quantities as actual quote |
| 009-plan-area-benchmark-quotation-v2 | execution | Plan area calc; new v2 file — do not edit v1 |
| 010-plan-2-area-quotation-qt-v1 | execution | Color-zone areas; dual quotation in new `qt-v1/` folder |
